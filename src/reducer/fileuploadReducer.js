const INITIAL_STATE = {
  file: []
}
const fileuploadReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return {
        state
      }
      break
  }
}

export default fileuploadReducer
